package fundamentos

/** Constantes
 * val -> value
 * Constante possui valor imutável, logo, o valor de uma constante não pode ser reatribuído (Val cannot be reassigned)
 */

fun main() {
    val a: Int = 1
    val b = 2 //Tipo Inferido

    //a = a + b
    print(a)
}